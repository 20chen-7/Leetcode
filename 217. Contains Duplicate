class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        dct = {}
        for i in nums:
            dct[i] = dct.get(i, 0)+1
            if dct[i] > 1:
                return True
        return False
